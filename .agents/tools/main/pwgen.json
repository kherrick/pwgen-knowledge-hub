{
  "name": "pwgen",
  "description": "Generate custom secure or phonetic passwords using the pwgen engine with configurable CLI flags, password length, and count.",
  "input_schema": {
    "type": "object",
    "properties": {
      "flags": {
        "type": "string",
        "description": "CLI flags for pwgen (e.g. '-sy', '-0', '-A', '-B', '-v'). Default is '-sy'."
      },
      "length": {
        "type": "number",
        "description": "Length of each password in characters. Default is 20."
      },
      "count": {
        "type": "number",
        "description": "Number of passwords to generate. Default is 1."
      }
    }
  },
  "execution": {
    "type": "javascript",
    "code": "const raw = typeof $PIPE_DATA !== 'undefined' && $PIPE_DATA ? (typeof $PIPE_DATA === 'string' ? JSON.parse($PIPE_DATA) : $PIPE_DATA) : (typeof data !== 'undefined' && data ? JSON.parse(data) : {}); let flags = '-sy'; let length = 20; let count = 1; let obj = {}; let str = ''; if (typeof raw === 'string') { str = raw.trim(); } else if (raw && typeof raw === 'object') { obj = raw; if (typeof raw.rawInput === 'string') str = raw.rawInput.trim(); else if (raw.rawInput && typeof raw.rawInput === 'object') obj = Object.assign({}, raw.rawInput, raw); } if (str && str.startsWith('{') && str.endsWith('}')) { try { const parsed = JSON.parse(str); if (parsed && typeof parsed === 'object') { obj = Object.assign({}, parsed, obj); str = ''; } } catch (e) {} } if (obj.flags !== undefined && obj.flags !== null && String(obj.flags).trim()) flags = String(obj.flags).trim(); if (obj.length !== undefined && obj.length !== null && !isNaN(Number(obj.length))) length = Number(obj.length); if (obj.count !== undefined && obj.count !== null && !isNaN(Number(obj.count))) count = Number(obj.count); else if (obj.number !== undefined && obj.number !== null && !isNaN(Number(obj.number))) count = Number(obj.number); if (str) { const flagMatches = str.match(/-[a-zA-Z0-9]+/g); if (flagMatches && flagMatches.length > 0) flags = flagMatches.join(''); const strippedStr = str.replace(/-[a-zA-Z0-9]+/g, ' '); const numberMatches = strippedStr.match(/\\b\\d+\\b/g); if (numberMatches && numberMatches.length > 0) { const parsedLen = Number(numberMatches[0]); if (!isNaN(parsedLen) && parsedLen > 0) length = parsedLen; if (numberMatches.length > 1) { const parsedCount = Number(numberMatches[1]); if (!isNaN(parsedCount) && parsedCount > 0) count = parsedCount; } } } length = Math.max(4, Math.min(1024, length)); count = Math.max(1, Math.min(100, count)); const isSecure = flags.includes('s'); const noCaps = flags.includes('A'); const noNums = flags.includes('0'); const incSymbols = flags.includes('y'); const noAmbiguous = flags.includes('B'); const noVowels = flags.includes('v'); let removeChars = ''; const rMatch = flags.match(/r([^\\s]+)/); if (rMatch) removeChars = rMatch[1]; const passwords = []; for (let c = 0; c < count; c++) { if (isSecure) { let chars = 'abcdefghijklmnopqrstuvwxyz'; if (!noCaps) chars += 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'; if (!noNums) chars += '0123456789'; if (incSymbols) chars += '!@#$%^&*()-_=+[]{}|;:,.<>?'; if (noAmbiguous) chars = chars.replace(/[0O1lI]/g, ''); if (noVowels) chars = chars.replace(/[aeiouAEIOU]/g, ''); if (removeChars) { for (const ch of removeChars) chars = chars.replaceAll(ch, ''); } if (!chars) chars = 'abcdefghijklmnopqrstuvwxyz'; const arr = new Uint32Array(length); crypto.getRandomValues(arr); let pwd = ''; for (let i = 0; i < length; i++) { pwd += chars[arr[i] % chars.length]; } passwords.push(pwd); } else { let consonants = 'bcdfghjklmnpqrstvwxyz'; let vowels = 'aeiou'; if (noVowels) vowels = '23456789'; if (noAmbiguous) { consonants = consonants.replace(/[l]/g, ''); vowels = vowels.replace(/[o]/g, ''); } const numbers = noNums ? '' : (noAmbiguous ? '23456789' : '0123456789'); const symbols = incSymbols ? '!@#$%^&*' : ''; let pwd = ''; let expectVowel = false; const rand = (max) => { const a = new Uint32Array(1); crypto.getRandomValues(a); return a[0] % max; }; while (pwd.length < length) { const remaining = length - pwd.length; if (!noNums && numbers && rand(5) === 0 && pwd.length > 0 && remaining > 1) { pwd += numbers[rand(numbers.length)]; expectVowel = rand(2) === 0; } else if (incSymbols && symbols && rand(7) === 0 && pwd.length > 0 && remaining > 1) { pwd += symbols[rand(symbols.length)]; expectVowel = rand(2) === 0; } else if (expectVowel) { pwd += vowels[rand(vowels.length)]; expectVowel = false; } else { let ch = consonants[rand(consonants.length)]; if (!noCaps && (pwd.length === 0 || rand(4) === 0)) ch = ch.toUpperCase(); pwd += ch; expectVowel = true; } } passwords.push(pwd.slice(0, length)); } } try { if (typeof BroadcastChannel !== 'undefined') { const bc = new BroadcastChannel('pwgen-commands'); bc.postMessage({ type: 'pwgen', params: { flags, length, count } }); bc.close(); } } catch (e) {} return '🔑 Generated Passwords:\\n```\\n' + passwords.join('\\n') + '\\n```\\n\\nFlags: ' + flags + ' | Length: ' + length + ' | Count: ' + count;"
  }
}
