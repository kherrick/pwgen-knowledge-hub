{
  "name": "pwgen_help",
  "description": "Explain pwgen command-line flags and options in human-readable detail.",
  "input_schema": {
    "type": "object",
    "properties": {
      "flags": {
        "type": "string",
        "description": "CLI flag string to explain (e.g. '-sy', '-0ABv'). If omitted or empty, explains all available pwgen flags."
      }
    }
  },
  "execution": {
    "type": "javascript",
    "code": "const input = typeof $PIPE_DATA !== 'undefined' && $PIPE_DATA ? (typeof $PIPE_DATA === 'string' ? JSON.parse($PIPE_DATA) : $PIPE_DATA) : (typeof data !== 'undefined' && data ? JSON.parse(data) : {}); let flags = (input.flags || input.rawInput || '').trim(); const allDoc = '📋 All Available pwgen CLI Flags Reference:\\n• 🔒 -s, --secure : Generate completely random passwords\\n• 🗣️ Default (without -s) : Generate pronounceable, phonetic passwords\\n• 🔤 -c, --capitalize : Include at least one capital letter (Default)\\n• 🚫 -A, --no-capitalize : Don\\'t include capital letters\\n• 🔢 -n, --numerals : Include at least one number (Default)\\n• 🚫 -0, --no-numerals : Don\\'t include numbers\\n• 🔣 -y, --symbols : Include at least one special symbol\\n• 👁️ -B, --ambiguous : Don\\'t include ambiguous characters (0, O, 1, l, I)\\n• 🙊 -v, --no-vowels : Avoid vowels to prevent offensive words\\n• ✂️ -r <chars> : Remove specific characters\\n• 📜 -1 : One per line'; const clean = flags.replace(/^-+/, '').trim(); if (!clean || clean.toLowerCase() === 'all' || clean.toLowerCase() === 'help') { return allDoc; } const exp = []; if (clean.includes('s')) exp.push('🔒 Pure Random (-s)'); else exp.push('🗣️ Phonetic Pronounceable (default)'); if (clean.includes('A')) exp.push('🚫 Capitals Excluded (-A)'); else exp.push('🔤 Capitals Included (default)'); if (clean.includes('0')) exp.push('🚫 Numbers Excluded (-0)'); else exp.push('🔢 Numbers Included (default)'); if (clean.includes('y')) exp.push('🔣 Special Symbols Included (-y)'); if (clean.includes('B')) exp.push('👁️ Ambiguous Omitted (-B)'); if (clean.includes('v')) exp.push('🙊 Vowels Excluded (-v)'); if (clean.includes('r')) exp.push('✂️ Custom Chars Removed (-r)'); return '📋 Flag Breakdown for \\'' + flags + '\\':\\n' + exp.join(', ');"
  }
}
